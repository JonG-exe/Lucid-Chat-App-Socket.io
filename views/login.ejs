<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>

    <style>

        * {
            font-family: "Poppins", sans-serif;
            margin: 0;
            padding: 0;
        }

        a {
            text-decoration: none;
        }

        a:visited {
            color: inherit;
        }

        body {
            width: 100vw;
            height: 100vh;
        }

        .container {
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;

            display: flex;
            justify-content: center;
            align-items: center;
            background-image: linear-gradient(130deg, rgba(1, 237, 245, 0.592) 30%, rgba(4, 55, 197, 0.96) 80%);
        }

        .login-container {
            width: 25%;
            height: 50%;
            display: flex;
            flex-direction: column;
            align-items: center;

            background-image: linear-gradient(to bottom right, rgba(163, 163, 163, 0.4), rgba(163, 163, 163, 0));
            border-radius: 27px;
            backdrop-filter: blur(4px);
            border: 1px solid rgba(255, 255, 255, 0.5);

            /* background-color: red; */

        }

        h1 {
            color: white;
            margin: 20px;
        }

        form {
            /* width: 100%; */
            width: 74%;

            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;

            /* background-color: red; */
        }

        input {
            box-sizing: border-box;
            width: 100%;
            height: 12%;
            margin: 10px;
            padding-left: 15px;
            background-color: transparent;
            background-image: linear-gradient(to bottom right, rgba(255, 255, 255, 0.6), rgb(255, 255, 255, 0.4));
            border-radius: 17px;
            border: 1px solid rgba(255, 255, 255, 0.5);

        }

        .login-button {
            cursor: pointer;
            font-size: 1.3em;
            width: 100%;
            height: 50px;
            margin: 15px;
            color: white;
            background-image: linear-gradient(to bottom right, rgba(255, 255, 255, 0.6), rgb(255, 255, 255, 0.4));
            border: 2px solid white;
            background-color: transparent;
        }

        .user_not_found {
            font-size: 1.5em;
            color: rgb(88, 86, 86);
            font-weight: 600;
            width: 100%;
            height: 50px;
            position: fixed;
            top: -100%;
            left: 0;
            /* background-color: rgb(255, 0, 0, 0.4); */
            background-image: linear-gradient(to bottom right, rgba(255, 255, 255, 0.515), rgba(255, 255, 255, 0.312));

            display: flex;
            justify-content: center;
            align-items: center;

            animation: user_not_found_anim 8s;
        }

        @keyframes user_not_found_anim{
            0% {
                top: -100%;
            }
            15% {
                top: 0;
            }
            80% {
                top: 0;
            }
            100% {
                top: -100%;
            }
        }
       

    </style>
</head>
<body>
    <div class="container">

        <% if(loginStatus === "user_not_found") { %>
              <div class="user_not_found">Username or Password was incorrect.</div> 
        <%  } %>

        <div class="login-container">
            <h1>Lucichat Login</h1>

            <form action="/login" method="POST">
        
                <input name="email" type="email" placeholder="Email">
                <input name="password" type="password" placeholder="Password">
        
                <button class="login-button">Login</button>
                
                <a href="/signup">Sign up</a>
            </form>
        </div>
    </div>
    
</body>
</html>